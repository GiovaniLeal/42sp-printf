# 42sp-printf
🖨️ ft_printf is a 42 School project that recreates the standard C library function printf. It aims to implement formatted output for strings, numbers, and characters to the standard output without relying on the original function. This repository also includes the bonus part, which adds extra formats and features.
